{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <div class="container">
        <div class="text font-weight-bold h2 mt-2 d-none d-md-block">Rubriques :</div>
 
                <div class="row ">

                    {% for rubriques in rubriques %}

                        <div class="col-4 mt-3 hover"> {{rubriques.nom}}<a href="{{ path('app_souscategories2', {'rubrique': rubriques.id})}}"> <img src="{{asset('images/'~rubriques.photo)}}" alt="{{rubriques.nom}}" class="img-fluid img-thumbnail"></a></div>
                       
                    {% endfor %}


                </div>
            </div>
        </div>
    </div>
    
</div>
{% endblock %}
