{% extends 'base.html.twig' %}

{% block title %}Sous Rubriques{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <div class="container">
        <div class="text font-weight-bold h2 mt-2 d-none d-md-block">Sous-Rubriques :</div>
 
                <div class="row ">

                    {% for sousrubriques in sousrubriques %}

                        <div class="col-4 mt-3 hover"> {{sousrubriques.nom}}<a href="/produits"> <img src="{{asset('images/'~sousrubriques.photo)}}" alt="{{sousrubriques.nom}}" class="img-fluid img-thumbnail"></a></div>
                       
                    {% endfor %}


                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
